<Page
    x:Class="QMSFlowDoc.Client.Views.DocumentsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:dto="using:QMSFlowDoc.Shared.DTOs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowDefinitions="Auto, *">
        <StackPanel Orientation="Horizontal" Spacing="20" Margin="0,0,0,24">
            <TextBlock Text="Gestión Documental" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <Button Style="{StaticResource AccentButtonStyle}" Click="AddDocument_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="16" />
                    <TextBlock Text="Nuevo Documento" />
                </StackPanel>
            </Button>
            <Button Click="AddFolder_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE8B7;" FontSize="16" />
                    <TextBlock Text="Nueva Carpeta" />
                </StackPanel>
            </Button>
            
            <CommandBar DefaultLabelPosition="Right" Background="Transparent" VerticalAlignment="Center">
                <AppBarButton Icon="Edit" Label="Editar" Click="EditSelected_Click" ToolTipService.ToolTip="Editar seleccionado"/>
                <AppBarButton Icon="Delete" Label="Eliminar" Click="DeleteSelected_Click" ToolTipService.ToolTip="Eliminar seleccionado"/>
            </CommandBar>

            <AutoSuggestBox x:Name="SearchBox" PlaceholderText="Buscar documentos..." QueryIcon="Find" Width="250" VerticalAlignment="Center" TextChanged="SearchBox_TextChanged" />
            <CheckBox x:Name="ShowObsoleteCheck" Content="Ver Obsoletos" VerticalAlignment="Center" Margin="10,0,0,0" Checked="ShowObsoleteCheck_Changed" Unchecked="ShowObsoleteCheck_Changed" />
        </StackPanel>

        <ProgressBar x:Name="LoadingBar" IsIndeterminate="True" Visibility="Collapsed" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="0,0,0,0" Grid.Row="1" Grid.ColumnSpan="2" Canvas.ZIndex="1" />

        <Grid Grid.Row="1" ColumnDefinitions="250, *">
            <!-- Sidebar: Folders -->
            <Border BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,1,0" Margin="0,0,16,0">
                <Grid RowDefinitions="Auto, *">
                    <TextBlock Text="Carpetas" Style="{StaticResource BaseTextBlockStyle}" Margin="0,0,0,8" FontWeight="Bold" />
                    <ListView x:Name="FoldersList" Grid.Row="1" SelectionMode="Single" SelectionChanged="FoldersList_SelectionChanged">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="ContextFlyout">
                                    <Setter.Value>
                                        <MenuFlyout>
                                            <MenuFlyoutItem Text="Renombrar Carpeta" Click="RenameFolder_Click" Icon="Edit"/>
                                            <MenuFlyoutItem Text="Eliminar Carpeta" Click="DeleteFolder_Click" Icon="Delete" Foreground="Red"/>
                                        </MenuFlyout>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="dto:FolderDto">
                                <StackPanel Orientation="Horizontal" Spacing="12" Padding="0,4">
                                    <FontIcon Glyph="&#xE8B7;" FontSize="16" Foreground="{ThemeResource SystemAccentColor}" />
                                    <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <!-- Main Content: Documents -->
            <StackPanel x:Name="EmptyStatePane" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16" Visibility="Collapsed" Canvas.ZIndex="2">
                <FontIcon Glyph="&#xE8B7;" FontSize="48" Foreground="Gray" />
                <TextBlock Text="No hay documentos en esta carpeta" FontSize="18" Foreground="Gray" />
            </StackPanel>

            <ListView Grid.Column="1" x:Name="DocumentsList"
                      ItemsSource="{x:Bind Documents, Mode=OneWay}"
                      SelectionMode="Single"
                      HorizontalContentAlignment="Stretch"
                      IsItemClickEnabled="False"
                      ItemClick="DocumentsList_ItemClick">
                <ListView.HeaderTemplate>
                    <DataTemplate>
                        <Grid Padding="12,4,48,4" HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="50" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="60" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Código" FontWeight="Bold" Grid.Column="0" Tapped="Sort_Tapped" Tag="DocCode" />
                            <TextBlock Text="Título" FontWeight="Bold" Grid.Column="1" Tapped="Sort_Tapped" Tag="Title" />
                            <TextBlock Text="Área" FontWeight="Bold" Grid.Column="2" Tapped="Sort_Tapped" Tag="Area" />
                            <TextBlock Text="Proceso" FontWeight="Bold" Grid.Column="3" Tapped="Sort_Tapped" Tag="Process" />
                            <TextBlock Text="Tipo" FontWeight="Bold" Grid.Column="4" Tapped="Sort_Tapped" Tag="TypeName" />
                            <TextBlock Text="Estado" FontWeight="Bold" Grid.Column="5" Tapped="Sort_Tapped" Tag="Status" />
                            <TextBlock Text="Ver." FontWeight="Bold" Grid.Column="6" Tapped="Sort_Tapped" Tag="CurrentVersionLabel" />
                            <TextBlock Text="Próx. Rev" FontWeight="Bold" Grid.Column="7" Tapped="Sort_Tapped" Tag="NextReviewDue" />
                            <TextBlock Text="Abrir" FontWeight="Bold" Grid.Column="8" HorizontalAlignment="Center" />
                            <TextBlock Text="Imp." FontWeight="Bold" Grid.Column="9" HorizontalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListView.HeaderTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="IsDoubleTapEnabled" Value="False" />
                        <Setter Property="ContextFlyout">
                            <Setter.Value>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="Editar Documento" Click="EditDocument_Click" Icon="Edit"/>
                                    <MenuFlyoutItem Text="Eliminar Documento" Click="DeleteDocument_Click" Icon="Delete" Foreground="Red"/>
                                </MenuFlyout>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="dto:DocumentDto">
                        <Grid Padding="12,8,48,8" HorizontalAlignment="Stretch" 
                              IsDoubleTapEnabled="False" 
                              DoubleTapped="Grid_DoubleTapped">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="50" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="60" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{x:Bind DocCode}" Grid.Column="0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{x:Bind Title}" Grid.Column="1" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{x:Bind Area}" Grid.Column="2" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{x:Bind Process}" Grid.Column="3" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{x:Bind TypeName}" Grid.Column="4" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{x:Bind Status.ToString()}" Grid.Column="5" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{x:Bind CurrentVersionLabel}" Grid.Column="6" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{x:Bind NextReviewDisplay}" Grid.Column="7" VerticalAlignment="Center" FontSize="12" Foreground="Gray" TextTrimming="CharacterEllipsis"/>
                            
                            <Button Grid.Column="8" Content="&#xE896;" FontFamily="Segoe MDL2 Assets" Click="OpenDocument_Click" ToolTipService.ToolTip="Abrir archivo" HorizontalAlignment="Center" Height="28" Width="36" Padding="0" />
                            <Button Grid.Column="9" Content="&#xE749;" FontFamily="Segoe MDL2 Assets" Click="PrintDocument_Click" ToolTipService.ToolTip="Imprimir copia controlada" HorizontalAlignment="Center" Height="28" Width="36" Padding="0" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
