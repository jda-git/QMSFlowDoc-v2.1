<Page
    x:Class="QMSFlowDoc.Client.Views.IssuesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:dto="using:QMSFlowDoc.Shared.DTOs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowDefinitions="Auto, *">
        <StackPanel Orientation="Horizontal" Spacing="20" Margin="0,0,0,24">
            <TextBlock Text="No Conformidades y Mejora" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <Button Content="Reportar No Conformidad" Style="{StaticResource AccentButtonStyle}" Click="AddNC_Click" />
            <ComboBox x:Name="StatusFilter" Header="Filtrar por Estado" SelectionChanged="StatusFilter_SelectionChanged" MinWidth="150" VerticalAlignment="Bottom">
                <ComboBoxItem Content="Todas" />
                <ComboBoxItem Content="Abiertas" IsSelected="True" />
                <ComboBoxItem Content="Cerradas" />
            </ComboBox>
        </StackPanel>

        <ListView Grid.Row="1" x:Name="IssuesList"
                  ItemsSource="{x:Bind Issues, Mode=OneWay}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="IssuesList_ItemClick">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="80" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Fecha Detección" FontWeight="Bold" Grid.Column="0" />
                        <TextBlock Text="Título" FontWeight="Bold" Grid.Column="1" />
                        <TextBlock Text="Severidad" FontWeight="Bold" Grid.Column="2" />
                        <TextBlock Text="Estado" FontWeight="Bold" Grid.Column="3" />
                         <TextBlock Text="Imp. Paciente" FontWeight="Bold" Grid.Column="4" />
                        <TextBlock Text="Acciones" FontWeight="Bold" Grid.Column="5" HorizontalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dto:NCListDto">
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="120" />
                             <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="80" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind DetectedAt.ToString()}" Grid.Column="0" />
                        <TextBlock Text="{x:Bind Title}" Grid.Column="1" />
                        <TextBlock Text="{x:Bind Severity.ToString()}" Grid.Column="2" />
                        <TextBlock Text="{x:Bind Status.ToString()}" Grid.Column="3" />
                         <TextBlock Text="{x:Bind ImpactPatient.ToString()}" Grid.Column="4" />
                        <TextBlock Text="{x:Bind ActionCount}" Grid.Column="5" HorizontalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
