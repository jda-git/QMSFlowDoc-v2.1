<Page
    x:Class="QMSFlowDoc.Client.Views.EquipmentEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24" MaxWidth="600" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Click="Back_Click">
                    <FontIcon Glyph="&#xE72B;" />
                </Button>
                <TextBlock Text="Nuevo Equipo" Style="{StaticResource TitleTextBlockStyle}" />
            </StackPanel>

            <Grid ColumnDefinitions="*, *" RowDefinitions="Auto, Auto, Auto, Auto, Auto" ColumnSpacing="16" RowSpacing="16">
                <TextBox x:Name="NameBox" Header="Nombre del Equipo" PlaceholderText="Centrífuga, Nevera..." Grid.Row="0" Grid.ColumnSpan="2" />
                
                <TextBox x:Name="AssetTagBox" Header="Etiqueta de Activo" PlaceholderText="EQ-LAB-001" Grid.Row="1" Grid.Column="0" />
                <TextBox x:Name="ManufacturerBox" Header="Fabricante" PlaceholderText="Thermo Fisher, etc." Grid.Row="1" Grid.Column="1" />
                
                <TextBox x:Name="ModelBox" Header="Modelo" Grid.Row="2" Grid.Column="0" />
                <TextBox x:Name="SerialBox" Header="Número de Serie" Grid.Row="2" Grid.Column="1" />
                
                <TextBox x:Name="LocationBox" Header="Ubicación" Grid.Row="3" Grid.ColumnSpan="2" />
                
                <DatePicker x:Name="InstallationDatePicker" Header="Fecha de Instalación" Grid.Row="4" Grid.Column="0" />
            </Grid>

            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,12,0,0">
                <Button Content="Registrar Equipo" Style="{StaticResource AccentButtonStyle}" Click="Save_Click" />
                <Button Content="Cancelar" Click="Back_Click" />
            </StackPanel>
            
            <TextBlock x:Name="ErrorText" Foreground="#E81123" Visibility="Collapsed" />
        </StackPanel>
    </ScrollViewer>
</Page>
