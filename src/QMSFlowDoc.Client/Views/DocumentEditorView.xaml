<Page
    x:Class="QMSFlowDoc.Client.Views.DocumentEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24" MaxWidth="600" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Click="Back_Click">
                    <FontIcon Glyph="&#xE72B;" />
                </Button>
                <TextBlock Text="Nuevo Documento" Style="{StaticResource TitleTextBlockStyle}" />
            </StackPanel>

            <Grid ColumnDefinitions="*, *" RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto" ColumnSpacing="16" RowSpacing="16">
                <TextBox x:Name="TitleBox" Header="Título del Documento" PlaceholderText="Ej: PNT Hematología" Grid.Row="0" Grid.ColumnSpan="2" />
                <TextBox x:Name="CodeBox" Header="Código (Ej: PRO-LAB-01)" PlaceholderText="PRO-001" Grid.Row="1" Grid.Column="0" />
                <ComboBox x:Name="TypeCombo" Header="Tipo de Documento" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch" DisplayMemberPath="Name" />
                <ComboBox x:Name="FolderCombo" Header="Carpeta de Destino" Grid.Row="2" Grid.Column="0" HorizontalAlignment="Stretch" DisplayMemberPath="Name" />
                <TextBox x:Name="VersionBox" Header="Versión (Ej: v1.0)" PlaceholderText="v1.0" Grid.Row="2" Grid.Column="1" />
                
                <TextBox x:Name="AreaBox" Header="Área / Departamento" Grid.Row="3" Grid.Column="0" />
                <TextBox x:Name="ProcessBox" Header="Proceso Relacionado" Grid.Row="3" Grid.Column="1" />
                
                <NumberBox x:Name="ReviewIntervalBox" Header="Intervalo de Revisión (meses)" Value="12" Grid.Row="4" Grid.Column="0" />
                <ComboBox x:Name="StatusCombo" Header="Estado del Documento" Grid.Row="4" Grid.Column="1" HorizontalAlignment="Stretch" />
                
                <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Spacing="8" Margin="0,12,0,0">
                    <TextBlock Text="Archivo del Documento (PDF/Word)" Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Button Content="Seleccionar Archivo" Click="Upload_Click" />
                        <TextBlock x:Name="FileNameText" Text="Ningún archivo seleccionado" VerticalAlignment="Center" FontStyle="Italic" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,12,0,0" Grid.Row="6" Grid.ColumnSpan="2">
                    <Button Content="Guardar Documento" Style="{StaticResource AccentButtonStyle}" Click="Save_Click" />
                    <Button Content="Cancelar" Click="Back_Click" />
                </StackPanel>
                
                <TextBlock x:Name="ErrorText" Foreground="#E81123" Visibility="Collapsed" Grid.Row="7" Grid.ColumnSpan="2" />
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
