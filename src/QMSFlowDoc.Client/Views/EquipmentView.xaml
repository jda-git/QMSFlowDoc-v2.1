<Page
    x:Class="QMSFlowDoc.Client.Views.EquipmentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:dto="using:QMSFlowDoc.Shared.DTOs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowDefinitions="Auto, *">
        <StackPanel Orientation="Horizontal" Spacing="20" Margin="0,0,0,24">
            <TextBlock Text="Equipos y Equipamiento" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <Button Content="Nuevo Equipo" Style="{StaticResource AccentButtonStyle}" Click="AddEquipment_Click" />
            <Button x:Name="EditEquipmentButton" Content="Editar Equipo" Click="EditEquipment_Click" IsEnabled="False" />
            <Button x:Name="DeleteEquipmentButton" Content="Eliminar Equipo" Click="DeleteEquipment_Click" IsEnabled="False" Foreground="Red" />
            <Button Content="Registrar Mantenimiento" Click="RegisterMaintenance_Click" />
            <Button Content="Registrar QC Diario" Click="RegisterQC_Click" Style="{StaticResource AccentButtonStyle}" />
            <Button Click="ExportExcel_Click">
                <Button.Content>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xEDE1;" FontSize="14" />
                        <TextBlock Text="Exportar Excel" />
                    </StackPanel>
                </Button.Content>
            </Button>

        </StackPanel>

        <ProgressBar x:Name="LoadingBar" IsIndeterminate="True" Visibility="Collapsed" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="0,0,0,0" Grid.Row="1" Canvas.ZIndex="1" />

        <StackPanel x:Name="EmptyStatePane" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16" Visibility="Collapsed">
            <FontIcon Glyph="&#xE9A2;" FontSize="48" Foreground="Gray" />
            <TextBlock Text="No hay equipos registrados" FontSize="18" Foreground="Gray" />
        </StackPanel>


        <ListView Grid.Row="1" x:Name="EquipmentList"
                  ItemsSource="{x:Bind Equipment, Mode=OneWay}"
                  SelectionMode="Single"
                  IsItemClickEnabled="False"
                  SelectionChanged="EquipmentList_SelectionChanged">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="80" />
                        </Grid.ColumnDefinitions>
                        <Button Content="Etiqueta" Click="Sort_AssetTag_Click" Style="{StaticResource TextBlockButtonStyle}" FontWeight="Bold" Grid.Column="0" />
                        <Button Content="Nombre" Click="Sort_Name_Click" Style="{StaticResource TextBlockButtonStyle}" FontWeight="Bold" Grid.Column="1" />
                        <TextBlock Text="Modelo" FontWeight="Bold" Grid.Column="2" VerticalAlignment="Center" Margin="12,0,0,0" />
                        <Button Content="Ubicación" Click="Sort_Location_Click" Style="{StaticResource TextBlockButtonStyle}" FontWeight="Bold" Grid.Column="3" />
                        <TextBlock Text="Últ. Mant." FontWeight="Bold" Grid.Column="4" VerticalAlignment="Center" Margin="12,0,0,0" />
                        <TextBlock Text="Tipo Evento" FontWeight="Bold" Grid.Column="5" VerticalAlignment="Center" Margin="12,0,0,0" />
                        <TextBlock Text="Resultado" FontWeight="Bold" Grid.Column="6" VerticalAlignment="Center" Margin="12,0,0,0" />
                        <TextBlock Text="Próx. Mant." FontWeight="Bold" Grid.Column="7" VerticalAlignment="Center" Margin="12,0,0,0" />
                        <TextBlock Text="Estado" FontWeight="Bold" Grid.Column="8" VerticalAlignment="Center" Margin="12,0,0,0" />
                        <TextBlock Text="QC Hoy" FontWeight="Bold" Grid.Column="9" VerticalAlignment="Center" Margin="12,0,0,0" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dto:EquipmentListDto">
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="80" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind AssetTag}" Grid.Column="0" />
                        <TextBlock Text="{x:Bind Name}" Grid.Column="1" />
                        <TextBlock Text="{x:Bind Model}" Grid.Column="2" Foreground="Gray" />
                        <TextBlock Text="{x:Bind Location}" Grid.Column="3" />
                        <TextBlock Text="{x:Bind LastMaintenanceDateFormatted}" Grid.Column="4" />
                        <TextBlock Text="{x:Bind LastEventType, FallbackValue='-'}" Grid.Column="5" Foreground="Gray" />
                        <TextBlock Text="{x:Bind LastOutcome, FallbackValue='-'}" Grid.Column="6" />
                        <TextBlock Text="{x:Bind NextMaintenanceDue, FallbackValue='-'}" Grid.Column="7" Foreground="Orange" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Bind StatusFormatted}" Grid.Column="8" />
                        <TextBlock Text="{x:Bind TodayQCStatus}" Grid.Column="9" FontWeight="Bold" 
                                   Foreground="{x:Bind TodayQCColor, Mode=OneWay}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ContentDialog x:Name="MaintenanceDialog" Title="Registrar Mantenimiento" PrimaryButtonText="Registrar" SecondaryButtonText="Cancelar" IsPrimaryButtonEnabled="True" PrimaryButtonClick="MaintenanceDialog_PrimaryButtonClick">
            <StackPanel Spacing="12" MinWidth="450">
                <ComboBox x:Name="MaintEquipmentCombo" Header="Equipo" DisplayMemberPath="Name" HorizontalAlignment="Stretch" SelectionChanged="MaintEquipmentCombo_SelectionChanged" />
                <DatePicker x:Name="MaintDatePicker" Header="Fecha de Mantenimiento" HorizontalAlignment="Stretch" />
                <ComboBox x:Name="MaintTypeCombo" Header="Tipo de Evento" HorizontalAlignment="Stretch">
                    <ComboBoxItem Content="Preventivo" Tag="PREVENTIVE" IsSelected="True" />
                    <ComboBoxItem Content="Correctivo" Tag="CORRECTIVE" />
                    <ComboBoxItem Content="Calibración" Tag="CALIBRATION" />
                </ComboBox>
                <CheckBox x:Name="MaintHasIssuesCheck" Content="¿Hubo incidencias durante el mantenimiento?" />
                <TextBox x:Name="MaintNotesBox" Header="Notas / Observaciones" AcceptsReturn="True" Height="80" />
                <ComboBox x:Name="MaintOutcomeCombo" Header="Resultado" HorizontalAlignment="Stretch">
                    <ComboBoxItem Content="Pass" IsSelected="True" />
                    <ComboBoxItem Content="Fail" />
                    <ComboBoxItem Content="Pending" />
                </ComboBox>
                <TextBlock Text="Próximo Mantenimiento" FontWeight="SemiBold" Margin="0,8,0,0" />
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <ComboBox x:Name="NextMaintMonthCombo" Header="Mes" Width="100">
                        <ComboBoxItem Content="01" Tag="1" />
                        <ComboBoxItem Content="02" Tag="2" />
                        <ComboBoxItem Content="03" Tag="3" />
                        <ComboBoxItem Content="04" Tag="4" />
                        <ComboBoxItem Content="05" Tag="5" />
                        <ComboBoxItem Content="06" Tag="6" />
                        <ComboBoxItem Content="07" Tag="7" />
                        <ComboBoxItem Content="08" Tag="8" />
                        <ComboBoxItem Content="09" Tag="9" />
                        <ComboBoxItem Content="10" Tag="10" />
                        <ComboBoxItem Content="11" Tag="11" />
                        <ComboBoxItem Content="12" Tag="12" />
                    </ComboBox>
                    <ComboBox x:Name="NextMaintYearCombo" Header="Año" Width="120" />
                </StackPanel>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>
