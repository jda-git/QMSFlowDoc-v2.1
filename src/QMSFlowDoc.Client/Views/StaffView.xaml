<Page
    x:Class="QMSFlowDoc.Client.Views.StaffView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:dto="using:QMSFlowDoc.Shared.DTOs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowDefinitions="Auto, *">
        <StackPanel Orientation="Horizontal" Spacing="20" Margin="0,0,0,24">
            <TextBlock Text="Personal y Formación" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <Button Content="Nueva Ficha de Personal" Style="{StaticResource AccentButtonStyle}" Click="AddStaff_Click" />
            <Button Content="EDITAR FICHA" Click="EditStaff_Click" />
            <Button Click="ExportExcel_Click">
                <Button.Content>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xEDE1;" FontSize="14" />
                        <TextBlock Text="Exportar Excel" />
                    </StackPanel>
                </Button.Content>
            </Button>

            <Button Content="BORRAR FICHA" Click="DeleteStaff_Click" />
            <Button Content="LIMPIAR REGISTROS" Click="PurgeUsers_Click" Foreground="Red" />
        </StackPanel>

        <ProgressBar x:Name="LoadingBar" IsIndeterminate="True" Visibility="Collapsed" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="0,0,0,0" Grid.Row="1" Canvas.ZIndex="1" />

        <StackPanel x:Name="EmptyStatePane" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16" Visibility="Collapsed">
            <FontIcon Glyph="&#xE716;" FontSize="48" Foreground="Gray" />
            <TextBlock Text="No hay personal registrado" FontSize="18" Foreground="Gray" />
        </StackPanel>


        <ListView Grid.Row="1" x:Name="StaffList"
                  ItemsSource="{x:Bind StaffMembers, Mode=OneWay}"
                  SelectionMode="Single">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="200" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Nombre Completo" FontWeight="Bold" Grid.Column="0" />
                        <TextBlock Text="Cargo" FontWeight="Bold" Grid.Column="1" />
                        <TextBlock Text="Departamento" FontWeight="Bold" Grid.Column="2" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dto:StaffListDto">
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="200" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind FullName}" Grid.Column="0" />
                        <TextBlock Text="{x:Bind PositionTitle}" Grid.Column="1" />
                        <TextBlock Text="{x:Bind Department}" Grid.Column="2" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
