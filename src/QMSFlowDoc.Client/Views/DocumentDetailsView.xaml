<Page
    x:Class="QMSFlowDoc.Client.Views.DocumentDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:models="using:QMSFlowDoc.Shared.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24" MaxWidth="800" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Click="Back_Click">
                    <FontIcon Glyph="&#xE72B;" />
                </Button>
                <TextBlock Text="Detalles del Documento" Style="{StaticResource TitleTextBlockStyle}" />
            </StackPanel>

            <Grid RowDefinitions="Auto, Auto, Auto, Auto, Auto" ColumnDefinitions="Auto, *" ColumnSpacing="20" RowSpacing="12">
                <TextBlock Text="Código:" FontWeight="Bold" Grid.Row="0" Grid.Column="0" />
                <TextBlock x:Name="CodeText" Grid.Row="0" Grid.Column="1" />

                <TextBlock Text="Título:" FontWeight="Bold" Grid.Row="1" Grid.Column="0" />
                <TextBlock x:Name="TitleText" Grid.Row="1" Grid.Column="1" />

                <TextBlock Text="Área:" FontWeight="Bold" Grid.Row="2" Grid.Column="0" />
                <TextBlock x:Name="AreaText" Grid.Row="2" Grid.Column="1" />

                <TextBlock Text="Proceso:" FontWeight="Bold" Grid.Row="3" Grid.Column="0" />
                <TextBlock x:Name="ProcessText" Grid.Row="3" Grid.Column="1" />

                <TextBlock Text="Estado Actual:" FontWeight="Bold" Grid.Row="4" Grid.Column="0" />
                <Border x:Name="StatusBadge" Padding="8,4" CornerRadius="4" Grid.Row="4" Grid.Column="1" HorizontalAlignment="Left">
                    <TextBlock x:Name="StatusText" Foreground="White" />
                </Border>
            </Grid>

            <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,12" />

            <TextBlock Text="Acciones de Ciclo de Vida" Style="{StaticResource SubtitleTextBlockStyle}" />
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button x:Name="ApproveButton" Content="Aprobar Documento" Style="{StaticResource AccentButtonStyle}" Click="Approve_Click" Visibility="Collapsed" />
                <Button x:Name="SendReviewButton" Content="Enviar a Revisión" Click="SendReview_Click" Visibility="Collapsed" />
                <Button x:Name="ObsoleteButton" Content="Marcar como Obsoleto" Click="Obsolete_Click" Visibility="Collapsed" />
            </StackPanel>

            <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,12" />

            <TextBlock Text="Acciones ISO 15189" Style="{StaticResource SubtitleTextBlockStyle}" />
            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
                <Button x:Name="OpenWatermarkButton" Click="OpenWithWatermark_Click" Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8A7;" />
                        <TextBlock Text="Abrir con Marca de Agua" />
                    </StackPanel>
                </Button>
                <Button x:Name="PrintCopyButton" Click="PrintControlledCopy_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE749;" />
                        <TextBlock Text="Imprimir Copia Controlada" />
                    </StackPanel>
                </Button>
                <Button x:Name="ExportPdfButton" Click="ExportMarkedPdf_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xEDE1;" />
                        <TextBlock Text="Exportar PDF Marcado" />
                    </StackPanel>
                </Button>
            </StackPanel>

            <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,12" />

            <TextBlock Text="Versiones" Style="{StaticResource SubtitleTextBlockStyle}" />
            <ListView x:Name="VersionsList" ItemsSource="{x:Bind Document.Versions, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:DocumentVersion">
                        <Grid Padding="0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="150" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{x:Bind VersionLabel}" FontWeight="Bold" />
                            <TextBlock Text="{x:Bind ChangeSummary}" Grid.Column="1" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{x:Bind CreatedAt}" Grid.Column="2" HorizontalAlignment="Right" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</Page>
