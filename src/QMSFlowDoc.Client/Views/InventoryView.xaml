<Page
    x:Class="QMSFlowDoc.Client.Views.InventoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:dto="using:QMSFlowDoc.Shared.DTOs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowDefinitions="Auto, *">
        <StackPanel Orientation="Horizontal" Spacing="20" Margin="0,0,0,24">
            <TextBlock Text="Inventario" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <CommandBar DefaultLabelPosition="Right" Background="Transparent">
                <AppBarButton Icon="Add" Label="Nuevo Reactivo" Click="AddReagent_Click" />
                <AppBarButton x:Name="ModifyButton" Icon="Edit" Label="Modificar Artículo" Click="ModifyReagent_Click" IsEnabled="False" />
                <AppBarSeparator />
                <AppBarButton Icon="Add" Label="Entrada (Lote)" Click="RegisterLot_Click" />
                <AppBarButton Icon="Remove" Label="Salida/Consumo" Click="RegisterExit_Click" />
                <AppBarButton Icon="Print" Label="Imprimir" Click="Print_Click" />
                <AppBarSeparator />
                <AppBarButton Icon="Delete" Label="BORRAR REGISTRO" Click="DeleteReagent_Click" Foreground="Red" />
            </CommandBar>
            
            <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                <ToggleSwitch x:Name="ActiveFilter" Header="Solo Activos" IsOn="True" Toggled="Filter_Toggled" OffContent="No" OnContent="Sí" />
                <ToggleSwitch x:Name="LowStockFilter" Header="Stock Bajo" IsOn="False" Toggled="Filter_Toggled" OffContent="No" OnContent="Sí" />
            </StackPanel>

            <AutoSuggestBox PlaceholderText="Buscar..." QueryIcon="Find" Width="200" VerticalAlignment="Center" TextChanged="SearchBox_TextChanged" />
        </StackPanel>

        <ListView Grid.Row="1" x:Name="ReagentsList"
                  ItemsSource="{x:Bind Reagents, Mode=OneWay}"
                  SelectionMode="Single"
                  IsItemClickEnabled="False"
                  SelectionChanged="ReagentsList_SelectionChanged">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <Button Content="Nombre" Click="Header_Click" Tag="Name" Grid.Column="0" FontWeight="Bold" Background="Transparent" HorizontalContentAlignment="Left" HorizontalAlignment="Stretch" BorderThickness="0" />
                        <Button Content="Fluorescencia" Click="Header_Click" Tag="Fluorescence" Grid.Column="1" FontWeight="Bold" Background="Transparent" HorizontalContentAlignment="Left" HorizontalAlignment="Stretch" BorderThickness="0" />
                        <Button Content="Código Interno" Click="Header_Click" Tag="InternalCode" Grid.Column="2" FontWeight="Bold" Background="Transparent" HorizontalContentAlignment="Left" HorizontalAlignment="Stretch" BorderThickness="0" />
                        <Button Content="Fabricante" Click="Header_Click" Tag="Manufacturer" Grid.Column="3" FontWeight="Bold" Background="Transparent" HorizontalContentAlignment="Left" HorizontalAlignment="Stretch" BorderThickness="0" />
                        <Button Content="Referencia" Click="Header_Click" Tag="Reference" Grid.Column="4" FontWeight="Bold" Background="Transparent" HorizontalContentAlignment="Left" HorizontalAlignment="Stretch" BorderThickness="0" />
                        <Button Content="LOTE-FECHA" Click="Header_Click" Tag="LotFechaSummary" Grid.Column="5" FontWeight="Bold" Background="Transparent" HorizontalContentAlignment="Left" HorizontalAlignment="Stretch" BorderThickness="0" />
                        <Button Content="Stock Total" Click="Header_Click" Tag="TotalStock" Grid.Column="6" FontWeight="Bold" Background="Transparent" HorizontalContentAlignment="Right" HorizontalAlignment="Stretch" BorderThickness="0" />
                        <Button Content="Objetivo" Click="Header_Click" Tag="TargetStock" Grid.Column="7" FontWeight="Bold" Background="Transparent" HorizontalContentAlignment="Right" HorizontalAlignment="Stretch" BorderThickness="0" />
                        <Button Content="Mínimo" Click="Header_Click" Tag="MinStock" Grid.Column="8" FontWeight="Bold" Background="Transparent" HorizontalContentAlignment="Right" HorizontalAlignment="Stretch" BorderThickness="0" />
                        <Button Content="Estado" Click="Header_Click" Tag="Status" Grid.Column="9" FontWeight="Bold" Background="Transparent" HorizontalContentAlignment="Right" HorizontalAlignment="Stretch" BorderThickness="0" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dto:ReagentListDto">
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind Name}" Grid.Column="0" />
                        <TextBlock Text="{x:Bind Fluorescence}" Grid.Column="1" FontStyle="Italic" />
                        <TextBlock Text="{x:Bind InternalCode}" Grid.Column="2" />
                        <TextBlock Text="{x:Bind Manufacturer}" Grid.Column="3" />
                        <TextBlock Text="{x:Bind Reference}" Grid.Column="4" />
                        <TextBlock Text="{x:Bind LotFechaSummary}" Grid.Column="5" Foreground="Gray" FontSize="12" TextWrapping="Wrap" />
                        <TextBlock Text="{x:Bind TotalStock.ToString()}" Grid.Column="6" FontWeight="SemiBold" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" HorizontalAlignment="Right" />
                        <TextBlock Text="{x:Bind TargetStock.ToString()}" Grid.Column="7" Foreground="Gray" HorizontalAlignment="Right" />
                        <TextBlock Text="{x:Bind MinStock.ToString()}" Grid.Column="8" HorizontalAlignment="Right" />
                        <TextBlock Text="{x:Bind Status}" Grid.Column="9" HorizontalAlignment="Right" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
