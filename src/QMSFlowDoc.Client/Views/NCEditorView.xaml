<Page
    x:Class="QMSFlowDoc.Client.Views.NCEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24" MaxWidth="600" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Click="Back_Click">
                    <FontIcon Glyph="&#xE72B;" />
                </Button>
                <TextBlock Text="Reportar No Conformidad" Style="{StaticResource TitleTextBlockStyle}" />
            </StackPanel>

            <StackPanel Spacing="16">
                <TextBox x:Name="TitleBox" Header="Título de la Incidencia" PlaceholderText="Ej: Error en etiquetado de muestras" />
                <TextBox x:Name="DescriptionBox" Header="Descripción de lo sucedido" PlaceholderText="Describa el hallazgo..." AcceptsReturn="True" Height="100" />
                
                <Grid ColumnDefinitions="*, *" RowDefinitions="Auto, Auto" ColumnSpacing="16" RowSpacing="12">
                    <ComboBox x:Name="SeverityCombo" Header="Gravedad" HorizontalAlignment="Stretch">
                        <ComboBoxItem Content="Observación" Tag="0" />
                        <ComboBoxItem Content="Menor" Tag="1" IsSelected="True" />
                        <ComboBoxItem Content="Mayor" Tag="2" />
                        <ComboBoxItem Content="Crítica" Tag="3" />
                    </ComboBox>
                    
                    <ComboBox x:Name="StatusCombo" Header="Estado" HorizontalAlignment="Stretch" Grid.Column="1">
                        <ComboBoxItem Content="OPEN" IsSelected="True" />
                        <ComboBoxItem Content="ANALYSIS" />
                        <ComboBoxItem Content="VERIFICATION" />
                        <ComboBoxItem Content="CLOSED" />
                    </ComboBox>

                    <CheckBox x:Name="ImpactPatientCheck" Content="¿Impacto en Paciente?" Grid.Row="1" />
                </Grid>

                <TextBox x:Name="ContainmentBox" Header="Acción Inmediata / Contención" PlaceholderText="¿Qué se hizo al momento?" AcceptsReturn="True" Height="80" />

                <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,12,0,0">
                    <Button Content="Enviar Reporte" Style="{StaticResource AccentButtonStyle}" Click="Save_Click" />
                    <Button Content="Cancelar" Click="Back_Click" />
                </StackPanel>
                
                <TextBlock x:Name="ErrorText" Foreground="#E81123" Visibility="Collapsed" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
