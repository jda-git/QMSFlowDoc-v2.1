<Page
    x:Class="QMSFlowDoc.Client.Views.ComplaintsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dto="using:QMSFlowDoc.Shared.DTOs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowDefinitions="Auto, *">
        <StackPanel Orientation="Horizontal" Spacing="20" Margin="0,0,0,24">
            <TextBlock Text="Quejas y Reclamaciones" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <Button Content="Registrar Queja" Style="{StaticResource AccentButtonStyle}" Click="AddComplaint_Click" />
            <ComboBox x:Name="StatusFilter" Header="Filtrar por Estado" SelectionChanged="StatusFilter_Changed" MinWidth="150" VerticalAlignment="Bottom">
                <ComboBoxItem Content="Todas" />
                <ComboBoxItem Content="Abiertas" IsSelected="True" />
                <ComboBoxItem Content="En Investigación" />
                <ComboBoxItem Content="Cerradas" />
            </ComboBox>
        </StackPanel>

        <ListView Grid.Row="1" x:Name="ComplaintsList"
                  ItemsSource="{x:Bind Complaints, Mode=OneWay}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="ComplaintsList_ItemClick">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Fecha" FontWeight="Bold" Grid.Column="0" />
                        <TextBlock Text="Origen" FontWeight="Bold" Grid.Column="1" />
                        <TextBlock Text="Descripción" FontWeight="Bold" Grid.Column="2" />
                        <TextBlock Text="Categoría" FontWeight="Bold" Grid.Column="3" />
                        <TextBlock Text="Estado" FontWeight="Bold" Grid.Column="4" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dto:ComplaintListDto">
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind Date.ToString()}" Grid.Column="0" />
                        <TextBlock Text="{x:Bind Source}" Grid.Column="1" />
                        <TextBlock Text="{x:Bind Description}" Grid.Column="2" TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{x:Bind Category.ToString()}" Grid.Column="3" />
                        <TextBlock Text="{x:Bind Status.ToString()}" Grid.Column="4" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
