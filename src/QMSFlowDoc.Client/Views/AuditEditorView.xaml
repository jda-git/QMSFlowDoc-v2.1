<Page
    x:Class="QMSFlowDoc.Client.Views.AuditEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:models="using:QMSFlowDoc.Shared.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24" MaxWidth="600" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Click="Back_Click">
                    <FontIcon Glyph="&#xE72B;" />
                </Button>
                <TextBlock Text="Planificar Auditoría Interna" Style="{StaticResource TitleTextBlockStyle}" />
            </StackPanel>

            <StackPanel Spacing="16">
                <TextBox x:Name="TitleBox" Header="Título / Referencia" PlaceholderText="Ej: Auditoría Q1 2024" />
                <DatePicker x:Name="ScheduledDatePicker" Header="Fecha Programada" />
                
                <TextBox x:Name="ScopeBox" Header="Alcance" PlaceholderText="Procesos a auditar..." AcceptsReturn="True" Height="80" />
                <TextBox x:Name="AuditorBox" Header="Auditor Líder" PlaceholderText="Nombre del auditor" />

                <Grid RowDefinitions="Auto, Auto" RowSpacing="8">
                    <TextBlock Text="Informe de Auditoría (Resultados)" FontWeight="SemiBold" />
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
                        <Button Content="Seleccionar Archivo (PDF/Word)" Click="UploadReport_Click" />
                        <Button x:Name="ViewReportButton" Content="Descargar" Click="ViewReport_Click" Visibility="Collapsed" />
                        <TextBlock x:Name="FileNameText" VerticalAlignment="Center" Foreground="Gray" Text="Ningún archivo seleccionado" />
                    </StackPanel>
                </Grid>

                <!-- Audit Checklist (ISO 15189 §8.8) -->
                <Expander Header="Checklist de Verificación" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" IsExpanded="True">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Button Content="Añadir Ítem" Click="AddChecklistItem_Click" Style="{StaticResource AccentButtonStyle}" />
                            <TextBlock x:Name="ChecklistCountText" VerticalAlignment="Center" Foreground="Gray" Text="0 ítems" />
                        </StackPanel>
                        <ListView x:Name="ChecklistListView" MaxHeight="300" BorderThickness="1" BorderBrush="#E0E0E0" CornerRadius="4">
                        </ListView>
                    </StackPanel>
                </Expander>

                <!-- Findings Section -->
                <Grid RowDefinitions="Auto, Auto">
                    <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,12,0,8">
                        <TextBlock Text="Hallazgos (Findings)" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" />
                        <Button Content="Registrar Hallazgo" Click="AddFinding_Click" Style="{StaticResource AccentButtonStyle}" />
                    </StackPanel>
                    
                    <ListView Grid.Row="1" x:Name="FindingsList" Height="150" BorderThickness="1" BorderBrush="#E0E0E0" CornerRadius="4">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:AuditFinding">
                                <Grid Padding="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="150" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{x:Bind Type}" FontWeight="Bold" Foreground="Gray" />
                                    <TextBlock Text="{x:Bind Description}" Grid.Column="1" Margin="12,0,0,0" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{x:Bind IsoRequirement}" Grid.Column="2" HorizontalAlignment="Right" Foreground="Blue" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>

                <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,12,0,0">
                    <Button Content="Guardar Auditoría" Style="{StaticResource AccentButtonStyle}" Click="Save_Click" />
                    <Button Content="Cancelar" Click="Back_Click" />
                </StackPanel>
                
                <TextBlock x:Name="ErrorText" Foreground="#E81123" Visibility="Collapsed" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
