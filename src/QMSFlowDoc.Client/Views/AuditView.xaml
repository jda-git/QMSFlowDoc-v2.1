<Page
    x:Class="QMSFlowDoc.Client.Views.AuditView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dto="using:QMSFlowDoc.Shared.DTOs"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Registro de Auditoría (Audit Log)" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,16" />

        <!-- Filters -->
        <!-- Filters -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Bottom">
                <CalendarDatePicker x:Name="StartDatePicker" Header="Desde" PlaceholderText="Fecha inicio" Width="140" />
                <CalendarDatePicker x:Name="EndDatePicker" Header="Hasta" PlaceholderText="Fecha fin" Width="140" />
                <TextBox x:Name="UserFilterBox" Header="Usuario" PlaceholderText="Filtrar por usuario..." Width="180" />
                <TextBox x:Name="ActionFilterBox" Header="Acción" PlaceholderText="Crear, Aprobar..." Width="180" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                <Button Click="ApplyFilters_Click" Style="{StaticResource AccentButtonStyle}" ToolTipService.ToolTip="Aplicar Filtros">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE71C;" FontSize="16" />
                        <TextBlock Text="Filtrar" />
                    </StackPanel>
                </Button>
                <Button Click="Refresh_Click" ToolTipService.ToolTip="Recargar Datos">
                    <FontIcon Glyph="&#xE72C;" FontSize="16" />
                </Button>
                <Button Click="Export_Click" ToolTipService.ToolTip="Exportar a CSV">
                    <FontIcon Glyph="&#xE718;" FontSize="16" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- DataGrid -->
        <!-- Header for List -->
        <Grid Grid.Row="2" VerticalAlignment="Top" Margin="0,0,12,0" Padding="12,8" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" BorderThickness="0,0,0,1" BorderBrush="#E0E0E0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Fecha" FontWeight="SemiBold"/>
            <TextBlock Text="Usuario" Grid.Column="1" FontWeight="SemiBold"/>
            <TextBlock Text="Acción" Grid.Column="2" FontWeight="SemiBold"/>
            <TextBlock Text="Entidad" Grid.Column="3" FontWeight="SemiBold"/>
            <TextBlock Text="ID" Grid.Column="4" FontWeight="SemiBold"/>
            <TextBlock Text="Detalles" Grid.Column="5" FontWeight="SemiBold"/>
        </Grid>

        <!-- List -->
        <ListView x:Name="AuditGrid" Grid.Row="2" Margin="0,40,0,0" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dto:AuditLogDto">
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind FormattedTimestamp}" />
                        <TextBlock Text="{x:Bind UserName}" Grid.Column="1" />
                        <TextBlock Text="{x:Bind Action}" Grid.Column="2" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Bind EntityType}" Grid.Column="3" />
                        <TextBlock Text="{x:Bind EntityId}" Grid.Column="4" FontSize="11" Foreground="Gray" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind Details}" Grid.Column="5" TextWrapping="Wrap" MaxLines="2" TextTrimming="CharacterEllipsis" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <ProgressBar x:Name="LoadingBar" IsIndeterminate="True" Visibility="Collapsed" HorizontalAlignment="Stretch" VerticalAlignment="Top" Grid.Row="2" />
        <TextBlock x:Name="EmptyStateText" Text="No se encontraron registros de auditoría con los filtros actuales." 
                   Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" 
                   Style="{StaticResource SubtitleTextBlockStyle}" Foreground="Gray" Visibility="Collapsed" />
    </Grid>
</Page>
