<Page
    x:Class="QMSFlowDoc.Client.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:dto="using:QMSFlowDoc.Shared.DTOs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="32">
            <TextBlock Text="Resumen General del Laboratorio" Style="{StaticResource TitleTextBlockStyle}" />
            <ProgressBar x:Name="LoadingBar" IsIndeterminate="True" Visibility="Collapsed" HorizontalAlignment="Stretch" Margin="0,0,0,16" />


            <!-- KPI Cards Grid -->
            <GridView x:Name="StatsGrid" SelectionMode="None" HorizontalAlignment="Stretch">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="5" />
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                
                <GridView.Items>
                    <Border Width="200" Height="100" CornerRadius="8" Background="#0078D4" Padding="16" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="Documentos" Foreground="White" Opacity="0.8" FontSize="12" />
                            <TextBlock Text="{x:Bind Stats.TotalDocuments, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="White" />
                        </StackPanel>
                    </Border>

                    <Border Width="200" Height="100" CornerRadius="8" Background="#E81123" Padding="16" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="Revisión Pendiente" Foreground="White" Opacity="0.8" FontSize="12" />
                            <TextBlock Text="{x:Bind Stats.PendingReviewDocs, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="White" />
                        </StackPanel>
                    </Border>

                    <Border Width="200" Height="100" CornerRadius="8" Background="#CA5010" Padding="16" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="Stock Bajo" Foreground="White" Opacity="0.8" FontSize="12" />
                            <TextBlock Text="{x:Bind Stats.LowStockReagents, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="White" />
                        </StackPanel>
                    </Border>

                    <Border Width="200" Height="100" CornerRadius="8" Background="#D83B01" Padding="16" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="Reactivos por Caducar" Foreground="White" Opacity="0.8" FontSize="12" />
                            <TextBlock Text="{x:Bind Stats.ExpiringReagents, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="White" />
                        </StackPanel>
                    </Border>

                    <Border Width="200" Height="100" CornerRadius="8" Background="#107C10" Padding="16" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="Mantenimiento Equipo" Foreground="White" Opacity="0.8" FontSize="12" />
                            <TextBlock Text="{x:Bind Stats.DueEquipmentMaintenance, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="White" />
                        </StackPanel>
                    </Border>

                    <Border Width="200" Height="100" CornerRadius="8" Background="#5C2D91" Padding="16" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="Programas EQA" Foreground="White" Opacity="0.8" FontSize="12" />
                            <TextBlock Text="{x:Bind Stats.ActiveEQAPrograms, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="White" />
                        </StackPanel>
                    </Border>

                    <Border Width="200" Height="100" CornerRadius="8" Background="#8764B8" Padding="16" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="EQA Pendientes" Foreground="White" Opacity="0.8" FontSize="12" />
                            <TextBlock Text="{x:Bind Stats.PendingEQAResults, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="White" />
                        </StackPanel>
                    </Border>

                    <Border Width="200" Height="100" CornerRadius="8" Background="#00B294" Padding="16" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="Métodos Activos" Foreground="White" Opacity="0.8" FontSize="12" />
                            <TextBlock Text="{x:Bind Stats.ActiveMethods, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="White" />
                        </StackPanel>
                    </Border>

                    <Border Width="200" Height="100" CornerRadius="8" Background="#E81123" Padding="16" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="Riesgos Críticos" Foreground="White" Opacity="0.8" FontSize="12" />
                            <TextBlock Text="{x:Bind Stats.OpenHighRisks, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="White" />
                        </StackPanel>
                    </Border>

                    <Border Width="200" Height="100" CornerRadius="8" Background="#0063B1" Padding="16" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="Personal Activo" Foreground="White" Opacity="0.8" FontSize="12" />
                            <TextBlock Text="{x:Bind Stats.ActiveStaffCount, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="White" />
                        </StackPanel>
                    </Border>
                </GridView.Items>
            </GridView>


            <!-- Activity and Tasks -->
            <Grid ColumnDefinitions="*, 400" ColumnSpacing="32">
                <StackPanel Grid.Column="0" Spacing="16">
                    <TextBlock Text="Actividad Reciente" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <ListView ItemsSource="{x:Bind RecentActivity, Mode=OneWay}" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="dto:DashboardRecentActivityDto">
                                <Grid Padding="0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="150" />
                                    </Grid.ColumnDefinitions>
                                    <SymbolIcon Symbol="Tag" VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                        <TextBlock Text="{x:Bind Description}" FontWeight="SemiBold" />
                                        <TextBlock Text="{x:Bind UserName}" FontSize="12" Opacity="0.6" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{x:Bind Timestamp.ToString()}" VerticalAlignment="Center" FontSize="12" Opacity="0.8" HorizontalAlignment="Right" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="16">
                    <TextBlock Text="⚠️ Alertas Urgentes" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <ListView x:Name="AlertsList" SelectionMode="None" MaxHeight="300">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}" CornerRadius="6" Padding="12,8" Margin="0,4">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Description}" FontSize="12" Opacity="0.8" TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <TextBlock x:Name="NoAlertsText" Text="✅ No hay alertas urgentes" Foreground="Green" FontStyle="Italic" Visibility="Collapsed" />
                </StackPanel>

            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
