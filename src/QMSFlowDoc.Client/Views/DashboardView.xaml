<Page
    x:Class="QMSFlowDoc.Client.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMSFlowDoc.Client.Views"
    xmlns:dto="using:QMSFlowDoc.Shared.DTOs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="32">
            <TextBlock Text="Resumen General del Laboratorio" Style="{StaticResource TitleTextBlockStyle}" />

            <!-- KPI Cards Grid -->
            <GridView x:Name="StatsGrid" SelectionMode="None" HorizontalAlignment="Stretch">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="4" />
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                
                <GridView.Items>
                    <Border Width="220" Height="120" CornerRadius="8" Background="{ThemeResource SystemControlBackgroundAccentBrush}" Padding="16" Margin="0,0,16,16">
                        <StackPanel>
                            <TextBlock Text="Documentos Totales" Foreground="White" Opacity="0.8" />
                            <TextBlock Text="{x:Bind Stats.TotalDocuments, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="White" />
                        </StackPanel>
                    </Border>

                    <Border Width="220" Height="120" CornerRadius="8" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Padding="16" Margin="0,0,16,16">
                        <StackPanel>
                            <TextBlock Text="Pendientes Revisión" Opacity="0.8" />
                            <TextBlock Text="{x:Bind Stats.PendingReviewDocs, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="#E81123" />
                        </StackPanel>
                    </Border>

                    <Border Width="220" Height="120" CornerRadius="8" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Padding="16" Margin="0,0,16,16">
                        <StackPanel>
                            <TextBlock Text="Reactivos Stock Bajo" Opacity="0.8" />
                            <TextBlock Text="{x:Bind Stats.LowStockReagents, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="#CA5010" />
                        </StackPanel>
                    </Border>

                    <Border Width="220" Height="120" CornerRadius="8" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Padding="16" Margin="0,0,16,16">
                        <StackPanel>
                            <TextBlock Text="Riesgos Críticos" Opacity="0.8" />
                            <TextBlock Text="{x:Bind Stats.OpenHighRisks, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="#E81123" />
                        </StackPanel>
                    </Border>
                </GridView.Items>
            </GridView>

            <!-- Activity and Tasks -->
            <Grid ColumnDefinitions="*, 400" ColumnSpacing="32">
                <StackPanel Grid.Column="0" Spacing="16">
                    <TextBlock Text="Actividad Reciente" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <ListView ItemsSource="{x:Bind RecentActivity, Mode=OneWay}" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="dto:DashboardRecentActivityDto">
                                <Grid Padding="0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="150" />
                                    </Grid.ColumnDefinitions>
                                    <SymbolIcon Symbol="Tag" VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                        <TextBlock Text="{x:Bind Description}" FontWeight="SemiBold" />
                                        <TextBlock Text="{x:Bind UserName}" FontSize="12" Opacity="0.6" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{x:Bind Timestamp.ToString()}" VerticalAlignment="Center" FontSize="12" Opacity="0.8" HorizontalAlignment="Right" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="16">
                    <TextBlock Text="Próximos Mantenimientos" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <Border Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" CornerRadius="8" Padding="16">
                        <TextBlock Text="No hay mantenimientos programados para hoy." FontStyle="Italic" Opacity="0.7" />
                    </Border>
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
